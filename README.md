# Hello-world python SUSE cloud native funadamentals

## This is simple hello-world python app, which is wrapped in Docker image. Also simple CI pipepline was created to be triggered via github actions